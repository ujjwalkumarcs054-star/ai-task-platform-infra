# DO NOT commit real secret values to git.
# Generate with: kubectl create secret generic app-secrets \
#   --namespace ai-task-platform \
#   --from-literal=JWT_SECRET=$(openssl rand -hex 32) \
#   --dry-run=client -o yaml > secret.yaml
#
# The stringData block below is a local-dev-only placeholder.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: ai-task-platform
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_ME_WITH_A_STRONG_RANDOM_SECRET"
